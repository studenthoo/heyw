# hyperai upstart service
description "hyperai server"
setuid www-data
start on started networking
start on runlevel [2345]
kill signal INT
respawn

env DIGITS_JOBS_DIR=/var/lib/hyperai/jobs
env DIGITS_LOGFILE_FILENAME=/var/log/hyperai/hyperai.log
exec python -m hyperai -p 34448
